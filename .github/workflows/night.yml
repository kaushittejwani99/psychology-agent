name: Night Eagle Audit
on:
  schedule:
    - cron: '0 17 * * *'
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Render Night URL
        run: curl -s "https://psychology-agent.onrender.com"