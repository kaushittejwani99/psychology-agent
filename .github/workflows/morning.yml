name: Morning Eagle Call
on:
  schedule:
    - cron: '30 3 * * *' 
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Render Morning URL
        run: curl -s "https://psychology-agent.onrender.com/trigger-morning"